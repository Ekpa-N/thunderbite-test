{
  "name": "thunderbite",
  "version": "1.0.0",
  "description": "neon sign animation + spinning wheel",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server && http-server -a localhost -p 4000 -o && nodemon server.js",
    "server":"nodemon server.js"
  },
  "author": "Ekpa",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "cors": "^2.8.5",
    "esm": "^3.2.25",
    "express": "^4.17.2"
  },
  "devDependencies": {
    "http-server": "^14.0.0",
    "live-server": "^1.2.1",
    "nodemon": "^2.0.15"
  }
}
